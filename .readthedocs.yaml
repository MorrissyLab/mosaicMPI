version: 2

build:
  os: "ubuntu-lts-latest"
  tools:
    python: "mambaforge-4.10"

# Build from the docs/ directory with Sphinx
sphinx:
  configuration: docs/source/conf.py

formats:
   - pdf

# Conda environment
conda:
  environment: docs/readthedocs_env.yml